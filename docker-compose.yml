services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - API_URL=${API_URL:-http://localhost:8080}
    container_name: seriestracker_frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:80"